Run the project test suite and report results.

**Permitted roles:** implementer, tester, orchestrator

Steps:
1. Identify the test command from the project configuration (e.g. `pytest`, `npm test`).
2. Run the tests and capture output.
3. Report pass/fail counts and any failure details.
